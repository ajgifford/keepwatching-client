import { Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container maxWidth="xl" sx={{ p: 4, display: 'flex', justifyContent: 'center' }}>
      <Typography variant="h2">KeepWatching!</Typography>
    </Container>
  );
};

export default Home;
